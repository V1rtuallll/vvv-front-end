# 移动端抽屉适配实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 让手机端（≤768px）不滚动就能触达导航（含 About）与音乐播放器，做法是把两个侧栏改成从两侧滑入的固定抽屉，PC 布局一行不改。

**Architecture:** 新增一个 `useDrawer()` composable 持有单一状态 `openDrawer`（`null | 'nav' | 'player'`），驱动两个既有 `<aside>` 的开合类名。抽屉的显隐**全部由 CSS `transform` 完成**，元素始终留在 DOM 中；触发按钮与遮罩只在 ≤768px 显示。桌面端的三栏 grid 与所有既有规则保持原样。

**Tech Stack:** Vue 3.5（`<script setup>`）、Vite 7、Vitest 5 + @vue/test-utils + jsdom、原生 CSS、pnpm。

## Global Constraints

以下约束适用于本计划中的每一个任务。

- **用户可见文案与注释一律中性**：陈述事实，不用「～」「哦」「啦」「呀」这类语气词，不用感叹号，不加颜文字。这条同时约束 `aria-label` 与 CSS/JS 注释。
- **缩进跟随所在文件**：本计划涉及的文件全部是 2 空格。
- **导入用 `@/` 别名**（指向 `src`），不写 `../../..`。
- **测试文件与被测文件同目录**，命名 `<名字>.test.js`。
- **新增或修改逻辑必须带测试**；纯样式改动豁免（本计划的 Task 4 因此没有测试）。
- **`window.$vmessage` 已由 `src/test/setup.js` 自动装上替身**，测试里直接断言，不要自己 mock。
- **测试命令是 `pnpm test`**（单次跑完）。不要用 npm 或 yarn。
- **不要擅自执行 `git commit` / `git push` / 切分支**（`AGENTS.md`「工作方式」）。本计划各任务末尾给出提交信息，**需要用户明确同意后才执行**；未获同意时该步骤跳过，改动留在工作区。
- **提交信息格式**：`<type>: <中文描述>`，type 保持英文，描述用中文，结尾不加句号。
- **不引入新依赖。** 本计划不使用 `@dicebear/*`、`boring-avatars` 或任何第三方头像库。

## 文件结构

| 动作 | 文件 | 职责 |
| --- | --- | --- |
| 新增 | `src/components/layout/useDrawer.js` | 抽屉开合状态、`Esc` 与视口回落关闭、`body` 滚动锁、卸载清理 |
| 新增 | `src/components/layout/useDrawer.test.js` | 上述逻辑的单元测试 |
| 改动 | `src/components/layout/DefaultLayout.vue` | 两个触发按钮、遮罩、两个 `<aside>` 的类名绑定、路由变化关闭 |
| 改动 | `src/components/layout/DefaultLayout.css` | 抽屉、按钮、遮罩样式；替换 ≤768px 块里已失效的排序与横向导航规则 |
| 改动 | `src/components/layout/DefaultLayout.test.js` | 挂载改用内存 router；补类名、`aria-expanded`、遮罩关闭、路由关闭、播放器 DOM 守卫 |
| 改动 | `AGENTS.md` | 修正「已知陷阱」里两条已过时的条目 |
| 已有 | `src/components/layout/doc/设计说明.md` | 本次改动的设计依据，不改 |
| 已有 | `src/utils/responsive.js` | 复用其 `isNarrowViewport()` 与 `NARROW_VIEWPORT_WIDTH`，不改 |

`useDrawer.js` 与 `useAudioPlayer.js` 是本仓库既有的 composable 写法：测试文件与被测文件同目录（`src/modules/player/composables/useAudioPlayer.js` 无测试，但 `src/modules/*/composables/*.test.js` 是通行做法）。`useDrawer.js` 放在 `components/layout/` 而不是 `modules/<域>/composables/`，因为它不属于任何业务域，只服务布局组件本身。

---

### Task 1: `useDrawer` 的开合状态与 body 滚动锁

**Files:**
- Create: `src/components/layout/useDrawer.js`
- Test: `src/components/layout/useDrawer.test.js`

**Interfaces:**
- Consumes: `isNarrowViewport(win, breakpoint)` 与 `NARROW_VIEWPORT_WIDTH`，来自 `@/utils/responsive`（已存在，`responsive.js:12` 与 `:32`）
- Produces: `useDrawer(win?)` 返回 `{ openDrawer, closeDrawer, toggleDrawer }`
  - `openDrawer: Ref<null | 'nav' | 'player'>`
  - `closeDrawer(): void`
  - `toggleDrawer(name: 'nav' | 'player'): void`
  - `win` 是可选注入参数，默认当前 `window`；测试不传，直接用 jsdom 的 `window`

- [ ] **Step 1: 写失败的测试**

创建 `src/components/layout/useDrawer.test.js`：

```js
import { mount } from "@vue/test-utils";
import { h, nextTick } from "vue";
import { afterEach, describe, expect, it } from "vitest";

import { useDrawer } from "@/components/layout/useDrawer";

// useDrawer 依赖 onBeforeUnmount，必须在组件上下文里调用
function mountDrawer() {
  let drawer;
  const wrapper = mount({
    setup() {
      drawer = useDrawer();
      return () => h("div");
    },
  });
  return { wrapper, drawer };
}

afterEach(() => {
  document.body.style.overflow = "";
});

describe("useDrawer 开合状态", () => {
  it("初始为收起，且不锁 body 滚动", () => {
    const { drawer } = mountDrawer();

    expect(drawer.openDrawer.value).toBe(null);
    expect(document.body.style.overflow).toBe("");
  });

  it("打开后记录当前抽屉名", () => {
    const { drawer } = mountDrawer();

    drawer.toggleDrawer("nav");

    expect(drawer.openDrawer.value).toBe("nav");
  });

  it("再次点击同一个按钮收起", () => {
    const { drawer } = mountDrawer();

    drawer.toggleDrawer("nav");
    drawer.toggleDrawer("nav");

    expect(drawer.openDrawer.value).toBe(null);
  });

  it("打开另一个时前一个自动关闭", () => {
    const { drawer } = mountDrawer();

    drawer.toggleDrawer("nav");
    drawer.toggleDrawer("player");

    expect(drawer.openDrawer.value).toBe("player");
  });

  it("打开时锁定 body 滚动，收起时还原", async () => {
    const { drawer } = mountDrawer();

    drawer.toggleDrawer("nav");
    await nextTick();
    expect(document.body.style.overflow).toBe("hidden");

    drawer.closeDrawer();
    await nextTick();
    expect(document.body.style.overflow).toBe("");
  });
});
```

- [ ] **Step 2: 运行测试确认失败**

Run: `pnpm test src/components/layout/useDrawer.test.js`
Expected: FAIL —— 模块不存在，报 `Failed to resolve import "@/components/layout/useDrawer"`

- [ ] **Step 3: 写最小实现**

创建 `src/components/layout/useDrawer.js`：

```js
import { onBeforeUnmount, ref, watch } from "vue";

/**
 * 移动端抽屉的开合状态。
 *
 * 抽屉只在 <=768px 存在：宽屏下两个触发按钮被 CSS 隐藏，状态不会被打开。
 * 单值设计让同一时刻最多只有一个抽屉展开。
 *
 * 显隐由 CSS transform 完成，元素始终留在 DOM 中 —— 播放器在 onMounted 里
 * 直接对 refs 取属性，元素一旦被 v-if 摘除，播放器会在所有分辨率下失效。
 *
 * 设计依据见 components/layout/doc/设计说明.md。
 */

const resolveWindow = () => (typeof window === "undefined" ? null : window);

export function useDrawer(win = resolveWindow()) {
  const openDrawer = ref(null);

  const closeDrawer = () => {
    openDrawer.value = null;
  };

  const toggleDrawer = (name) => {
    openDrawer.value = openDrawer.value === name ? null : name;
  };

  watch(openDrawer, (value) => {
    const body = win?.document?.body;
    if (body) body.style.overflow = value ? "hidden" : "";
  });

  onBeforeUnmount(() => {
    const body = win?.document?.body;
    if (body) body.style.overflow = "";
  });

  return { openDrawer, closeDrawer, toggleDrawer };
}
```

- [ ] **Step 4: 运行测试确认通过**

Run: `pnpm test src/components/layout/useDrawer.test.js`
Expected: PASS，5 个用例全绿

- [ ] **Step 5: 提交**

**需用户明确同意后执行**（`AGENTS.md`「不要擅自执行 git commit」）：

```bash
git add src/components/layout/useDrawer.js src/components/layout/useDrawer.test.js
git commit -m "feat: 新增移动端抽屉的开合状态"
```

---

### Task 2: `Esc` 关闭、视口回落关闭与卸载清理

**Files:**
- Modify: `src/components/layout/useDrawer.js`
- Test: `src/components/layout/useDrawer.test.js`

**Interfaces:**
- Consumes: Task 1 的 `useDrawer(win?)` 与它返回的三个成员
- Produces: `useDrawer` 的返回值形状不变，新增 `keydown` 与 `resize` 两个监听及其卸载清理

- [ ] **Step 1: 写失败的测试**

在 `src/components/layout/useDrawer.test.js` 顶部的 import 之后、`afterEach` 之前，补三个辅助函数：

```js
function setViewportWidth(width) {
  Object.defineProperty(window, "innerWidth", {
    configurable: true,
    writable: true,
    value: width,
  });
}

function pressEscape() {
  window.dispatchEvent(new KeyboardEvent("keydown", { key: "Escape" }));
}

function fireResize() {
  window.dispatchEvent(new Event("resize"));
}
```

在文件末尾追加：

```js
describe("useDrawer 关闭触发", () => {
  it("按 Esc 收起", async () => {
    const { drawer } = mountDrawer();
    drawer.toggleDrawer("player");
    await nextTick();

    pressEscape();

    expect(drawer.openDrawer.value).toBe(null);
  });

  it("视口回到宽屏时收起", async () => {
    const { drawer } = mountDrawer();
    setViewportWidth(400);
    drawer.toggleDrawer("nav");
    await nextTick();

    setViewportWidth(1024);
    fireResize();

    expect(drawer.openDrawer.value).toBe(null);
  });

  it("仍在窄屏时 resize 不收起", async () => {
    const { drawer } = mountDrawer();
    setViewportWidth(400);
    drawer.toggleDrawer("nav");
    await nextTick();

    setViewportWidth(360);
    fireResize();

    expect(drawer.openDrawer.value).toBe("nav");
  });
});

describe("useDrawer 卸载清理", () => {
  it("卸载后不再响应 Esc", async () => {
    const { wrapper, drawer } = mountDrawer();
    drawer.toggleDrawer("nav");
    await nextTick();

    wrapper.unmount();
    pressEscape();

    expect(drawer.openDrawer.value).toBe("nav");
  });

  it("卸载后不再响应 resize", async () => {
    const { wrapper, drawer } = mountDrawer();
    setViewportWidth(400);
    drawer.toggleDrawer("nav");
    await nextTick();

    wrapper.unmount();
    setViewportWidth(1024);
    fireResize();

    expect(drawer.openDrawer.value).toBe("nav");
  });

  it("卸载后还原 body 滚动锁", async () => {
    const { wrapper, drawer } = mountDrawer();
    drawer.toggleDrawer("nav");
    await nextTick();
    expect(document.body.style.overflow).toBe("hidden");

    wrapper.unmount();

    expect(document.body.style.overflow).toBe("");
  });
});
```

- [ ] **Step 2: 运行测试确认失败**

Run: `pnpm test src/components/layout/useDrawer.test.js`
Expected: FAIL 6 个 —— 前三项「视口回到宽屏时收起」等断言失败，后三项因监听未移除而失败

- [ ] **Step 3: 补上监听与清理**

把 `src/components/layout/useDrawer.js` 的 `useDrawer` 函数体替换为下面这版（`import` 行改成同时引入 `isNarrowViewport`）：

```js
import { onBeforeUnmount, ref, watch } from "vue";

import { isNarrowViewport } from "@/utils/responsive";

/**
 * 移动端抽屉的开合状态。
 *
 * 抽屉只在 <=768px 存在：宽屏下两个触发按钮被 CSS 隐藏，状态不会被打开。
 * 单值设计让同一时刻最多只有一个抽屉展开。视口回到宽屏时必须主动关闭 ——
 * 否则会残留遮罩与 body 滚动锁，而那时按钮不可见，用户无法自救。
 *
 * 显隐由 CSS transform 完成，元素始终留在 DOM 中 —— 播放器在 onMounted 里
 * 直接对 refs 取属性，元素一旦被 v-if 摘除，播放器会在所有分辨率下失效。
 *
 * 设计依据见 components/layout/doc/设计说明.md。
 */

const resolveWindow = () => (typeof window === "undefined" ? null : window);

export function useDrawer(win = resolveWindow()) {
  const openDrawer = ref(null);

  const closeDrawer = () => {
    openDrawer.value = null;
  };

  const toggleDrawer = (name) => {
    openDrawer.value = openDrawer.value === name ? null : name;
  };

  const onKeydown = (event) => {
    if (event.key === "Escape") closeDrawer();
  };

  const onResize = () => {
    if (!isNarrowViewport(win)) closeDrawer();
  };

  if (win) {
    win.addEventListener("keydown", onKeydown);
    win.addEventListener("resize", onResize);
  }

  watch(openDrawer, (value) => {
    const body = win?.document?.body;
    if (body) body.style.overflow = value ? "hidden" : "";
  });

  onBeforeUnmount(() => {
    if (win) {
      win.removeEventListener("keydown", onKeydown);
      win.removeEventListener("resize", onResize);
    }
    const body = win?.document?.body;
    if (body) body.style.overflow = "";
  });

  return { openDrawer, closeDrawer, toggleDrawer };
}
```

- [ ] **Step 4: 运行测试确认通过**

Run: `pnpm test src/components/layout/useDrawer.test.js`
Expected: PASS，11 个用例全绿

- [ ] **Step 5: 跑全量测试确认没有影响别处**

Run: `pnpm test`
Expected: PASS，全绿（基线为 150 个前端用例，此处应不少于该数）

- [ ] **Step 6: 提交**

**需用户明确同意后执行**：

```bash
git add src/components/layout/useDrawer.js src/components/layout/useDrawer.test.js
git commit -m "feat: 抽屉支持 Esc 与视口回落关闭并清理监听"
```

---

### Task 3: `DefaultLayout.vue` 接入抽屉

**Files:**
- Modify: `src/components/layout/DefaultLayout.vue`（模板 `28-90` 行、脚本 `115-132` 行）
- Test: `src/components/layout/DefaultLayout.test.js`

**Interfaces:**
- Consumes: Task 2 的 `useDrawer()` → `{ openDrawer, closeDrawer, toggleDrawer }`
- Produces: 供 Task 4 选择样式的 DOM 契约 ——
  - 两个按钮：`.drawer-toggle.nav`（`aria-label="打开导航"`）、`.drawer-toggle.player`（`aria-label="打开音乐播放器"`），均带 `aria-expanded`
  - 遮罩：`.drawer-backdrop`，点击调用 `closeDrawer`
  - 两个侧栏：`.sidebar.left` / `.sidebar.right`，展开时带类名 `is-open`

- [ ] **Step 1: 改写测试挂载，写失败的测试**

`useRoute()` 在组件外调用会返回 `undefined`，因此挂载必须装内存 router。把 `src/components/layout/DefaultLayout.test.js` 整个替换为：

```js
import { mount } from "@vue/test-utils";
import { createMemoryHistory, createRouter } from "vue-router";
import { beforeEach, describe, expect, it, vi } from "vitest";

vi.mock("@/modules/user/api/userApi", () => ({
  getUserCount: vi.fn().mockResolvedValue({ data: 1 }),
}));

// 刻意不 mock useAudioPlayer。原文件把它整体换成了空 ref，那恰好会掩盖
// 「给侧栏加 v-if 导致播放器全端失效」这个回归 —— 空 ref 让播放器的
// onMounted 逻辑永远不会真正执行。已实测：真实 composable 在 jsdom 里
// 挂载正常，不会抛异常，因此这里直接用真的，守卫用例才有意义。
import DefaultLayout from "@/components/layout/DefaultLayout.vue";

const stubs = {
  "router-link": { props: ["to"], template: '<a :href="to"><slot /></a>' },
  "router-view": true,
};

const routes = [
  { path: "/home", component: { template: "<div />" } },
  { path: "/profile", component: { template: "<div />" } },
  { path: "/gallery", component: { template: "<div />" } },
  { path: "/about", component: { template: "<div />" } },
];

let router;

async function mountLayout() {
  router = createRouter({ history: createMemoryHistory(), routes });
  await router.push("/home");
  await router.isReady();

  return mount(DefaultLayout, { global: { plugins: [router], stubs } });
}

// 去掉路由切换带来的异步：watch 默认在 pre 阶段刷新
const flush = () => new Promise((resolve) => setTimeout(resolve, 0));

describe("左侧导航", () => {
  it("有 About 入口，指向 /about", async () => {
    const wrapper = await mountLayout();

    const links = wrapper.findAll(".vf-nav a").map((a) => [a.text(), a.attributes("href")]);

    expect(links).toContainEqual(["About", "/about"]);
  });

  it("原有的三个入口顺序不变", async () => {
    const wrapper = await mountLayout();

    const labels = wrapper.findAll(".vf-nav a").map((a) => a.text());

    expect(labels.slice(0, 3)).toEqual(["Home", "Profile", "Gallery"]);
  });
});

describe("抽屉", () => {
  let wrapper;

  beforeEach(async () => {
    wrapper = await mountLayout();
  });

  it("初始全部收起，两个侧栏仍在 DOM 中", () => {
    expect(wrapper.find(".sidebar.left").classes()).not.toContain("is-open");
    expect(wrapper.find(".sidebar.right").classes()).not.toContain("is-open");
    expect(wrapper.find(".sidebar.left").exists()).toBe(true);
    expect(wrapper.find(".sidebar.right").exists()).toBe(true);
  });

  it("点导航按钮展开导航抽屉", async () => {
    await wrapper.find(".drawer-toggle.nav").trigger("click");

    expect(wrapper.find(".sidebar.left").classes()).toContain("is-open");
    expect(wrapper.find(".sidebar.right").classes()).not.toContain("is-open");
  });

  it("点播放器按钮展开播放器抽屉，导航自动收起", async () => {
    await wrapper.find(".drawer-toggle.nav").trigger("click");
    await wrapper.find(".drawer-toggle.player").trigger("click");

    expect(wrapper.find(".sidebar.right").classes()).toContain("is-open");
    expect(wrapper.find(".sidebar.left").classes()).not.toContain("is-open");
  });

  it("再点同一个按钮收起", async () => {
    await wrapper.find(".drawer-toggle.nav").trigger("click");
    await wrapper.find(".drawer-toggle.nav").trigger("click");

    expect(wrapper.find(".sidebar.left").classes()).not.toContain("is-open");
  });

  it("按钮的 aria-expanded 随开合变化", async () => {
    const toggle = wrapper.find(".drawer-toggle.nav");
    expect(toggle.attributes("aria-expanded")).toBe("false");

    await toggle.trigger("click");
    expect(toggle.attributes("aria-expanded")).toBe("true");
  });

  it("点遮罩关闭", async () => {
    await wrapper.find(".drawer-toggle.nav").trigger("click");

    await wrapper.find(".drawer-backdrop").trigger("click");

    expect(wrapper.find(".sidebar.left").classes()).not.toContain("is-open");
  });

  it("路由变化时关闭", async () => {
    await wrapper.find(".drawer-toggle.nav").trigger("click");
    expect(wrapper.find(".sidebar.left").classes()).toContain("is-open");

    await router.push("/gallery");
    await flush();

    expect(wrapper.find(".sidebar.left").classes()).not.toContain("is-open");
  });
});

describe("播放器元素常驻", () => {
  // 守卫用例。针对一个会让播放器在所有分辨率下失效的改法：给侧栏或播放器
  // 加 v-if。useAudioPlayer 在 onMounted 里直接对 8 个 ref 取属性
  // （audio.volume = 0.3、playButton.addEventListener(...)），任何一个为
  // null 就抛异常。所以抽屉的显隐只能靠 CSS transform，元素必须常驻 DOM。
  it("抽屉收起时播放器元素仍在 DOM 中", async () => {
    const wrapper = await mountLayout();

    expect(wrapper.find(".music-player audio").exists()).toBe(true);
    expect(wrapper.find(".music-player .player-controls").exists()).toBe(true);
    expect(wrapper.find(".sidebar.left .vf-nav").exists()).toBe(true);
  });
});
```

- [ ] **Step 2: 运行测试确认失败**

Run: `pnpm test src/components/layout/DefaultLayout.test.js`
Expected: FAIL —— 找不到 `.drawer-toggle.nav`、`.drawer-backdrop`，且 `useRoute()` 返回 `undefined` 导致访问 `route.fullPath` 抛错

- [ ] **Step 3: 改模板**

在 `src/components/layout/DefaultLayout.vue` 里做四处改动。

**3a.** 在 `</header>` 之后、`<div class="vf-container">` 之前插入两个触发按钮与遮罩：

```html
    <button
      class="drawer-toggle nav"
      type="button"
      aria-label="打开导航"
      :aria-expanded="openDrawer === 'nav'"
      @click="toggleDrawer('nav')"
    >
      ☰
    </button>

    <button
      class="drawer-toggle player"
      type="button"
      aria-label="打开音乐播放器"
      :aria-expanded="openDrawer === 'player'"
      @click="toggleDrawer('player')"
    >
      ♪
    </button>
```

**3b.** 给左侧栏加类名绑定：

```html
      <aside class="sidebar left" :class="{ 'is-open': openDrawer === 'nav' }">
```

**3c.** 给右侧栏加类名绑定（`<aside class="sidebar right">` 那一行）：

```html
      <aside class="sidebar right" :class="{ 'is-open': openDrawer === 'player' }">
```

**3d.** 在 `</div>`（`.vf-container` 的闭合标签）之后、`<footer class="neon-footer">` 之前插入遮罩：

```html
    <div v-show="openDrawer" class="drawer-backdrop" @click="closeDrawer"></div>
```

- [ ] **Step 4: 改脚本**

把 `src/components/layout/DefaultLayout.vue` 的 `<script setup>` 替换为：

```js
import { onMounted, ref, watch } from "vue";
import { useRoute } from "vue-router";

import { useDrawer } from "@/components/layout/useDrawer";
import { useAudioPlayer } from "@/modules/player/composables/useAudioPlayer";
import { getUserCount } from "@/modules/user/api/userApi";

const userCountEl = ref(null);
const route = useRoute();
const { openDrawer, closeDrawer, toggleDrawer } = useDrawer();
const { audioEl, playPauseBtn, prevBtn, nextBtn, progressBar, volumeSlider, trackName, volumeDisplay } = useAudioPlayer();

// 抽屉里点导航即跳转，跳转后必须收起，否则遮罩会留在新页面上
watch(() => route.fullPath, closeDrawer);

onMounted(async () => {
  try {
    const res = await getUserCount();
    if (userCountEl.value) userCountEl.value.textContent = res.data;
  } catch (error) {
    console.error("获取人数失败:", error);
    if (userCountEl.value) userCountEl.value.textContent = "???";
  }
});
```

`watch(() => route.fullPath, closeDrawer)` 放在 `useDrawer()` 之后：`closeDrawer` 在组件卸载时随 `useDrawer` 的 `onBeforeUnmount` 一起清理，监听由 Vue 自动解绑。

- [ ] **Step 5: 运行测试确认通过**

Run: `pnpm test src/components/layout/DefaultLayout.test.js`
Expected: PASS，10 个用例全绿

- [ ] **Step 6: 提交**

**需用户明确同意后执行**：

```bash
git add src/components/layout/DefaultLayout.vue src/components/layout/DefaultLayout.test.js
git commit -m "feat: 布局接入移动端抽屉与触发按钮"
```

---

### Task 4: 抽屉样式与 ≤768px 规则替换

**Files:**
- Modify: `src/components/layout/DefaultLayout.css`（新增一节；替换 `1347-1383` 行）

**Interfaces:**
- Consumes: Task 3 建立的 DOM 契约 —— `.drawer-toggle.nav` / `.drawer-toggle.player` / `.drawer-backdrop` / `.sidebar.is-open`
- Produces: 无代码接口，只产出视觉效果

**这个任务没有自动化测试**：`AGENTS.md`「测试要求」明确「纯样式、颜色、文案、错别字改动豁免」，`pnpm test` 不在本任务范围内。验收靠 Step 4 的手工检查。

- [ ] **Step 1: 新增抽屉样式节**

在 `src/components/layout/DefaultLayout.css` 里找到 `/* ==== 窄屏适配 ====` 这段注释的开头（大约在第 1278 行），在它**之前**插入：

```css
/* ==== 移动端抽屉 ====
   两个侧栏在 <=768px 变成从两侧滑入的固定面板，桌面端完全不受影响：
   下面这些规则里 .drawer-toggle 默认 display: none，.drawer-backdrop 在
   宽屏被显式隐藏，.sidebar 的定位只在 media query 内被覆盖。
*/
.drawer-toggle {
  display: none;
  position: fixed;
  top: 12px;
  z-index: 940;
  width: 44px;
  height: 44px;
  align-items: center;
  justify-content: center;
  font-size: 1.4rem;
  color: #00ffff;
  background: rgba(10, 0, 20, 0.9);
  border: 1px solid #00ffff88;
  border-radius: 10px;
  backdrop-filter: blur(6px);
  cursor: pointer;
}

.drawer-toggle.nav {
  left: 12px;
}

.drawer-toggle.player {
  right: 12px;
}

.drawer-backdrop {
  position: fixed;
  inset: 0;
  z-index: 900;
  background: rgba(0, 0, 0, 0.72);
  backdrop-filter: blur(2px);
}

/* 宽屏下抽屉本不该被打开，这里再隐藏一次是冗余保险：
   万一开合状态因异常残留，桌面上也不会留下遮罩 */
@media (min-width: 769px) {
  .drawer-backdrop {
    display: none;
  }
}
```

- [ ] **Step 2: 替换 ≤768px 块里已失效的规则**

在 `@media (max-width: 768px)` 块内，把下面这一段（含注释，从 `/* 单栏纵向排列 */` 到 `.left .nav-link { ... }` 的闭合花括号）：

```css
  /* 单栏纵向排列：主内容排在左右侧栏之前 */
  .vf-container {
    display: flex;
    flex-direction: column;
    gap: 16px;
    padding: 16px;
  }

  .vf-main {
    order: 1;
    padding: 16px;
    border-radius: 14px;
  }

  .sidebar.left {
    order: 2;
  }

  .sidebar.right {
    order: 3;
  }

  /* 导航改为横向排列，避免长时间纵向占位 */
  .left .vf-nav {
    display: flex;
    flex-wrap: wrap;
    gap: 8px;
  }

  .left .nav-link {
    flex: 1 1 96px;
    margin: 0;
    min-height: 44px;
    display: flex;
    align-items: center;
    justify-content: center;
  }
```

整段替换为：

```css
  /* 单栏纵向排列。两个侧栏已改为 fixed 抽屉、脱离文档流，
     这里只剩 .vf-main 参与排布，不再需要 order 排序。 */
  .vf-container {
    display: flex;
    flex-direction: column;
    gap: 16px;
    padding: 16px;
  }

  .vf-main {
    padding: 16px;
    border-radius: 14px;
  }

  /* 抽屉本体。收起用 transform 位移而不是 v-if —— useAudioPlayer 在
     onMounted 里直接对 refs 取属性，元素一旦不在 DOM 中，播放器会在
     所有分辨率下失效。收起态额外加 visibility: hidden，把仍停在视口外
     的整棵子树移出 Tab 顺序；visibility 是可过渡的离散属性，收起时在
     滑出动画结束后才生效，展开时立即显示。 */
  .sidebar {
    position: fixed;
    top: 0;
    bottom: 0;
    width: min(320px, 84vw);
    overflow-x: hidden;
    overflow-y: auto;
    overscroll-behavior: contain;
    /* 为刘海屏与底部指示条留白 */
    padding-bottom: calc(25px + env(safe-area-inset-bottom));
    z-index: 920;
    visibility: hidden;
    transition: transform 0.28s ease, visibility 0.28s ease;
    /* 这一行必须保留：.sidebar 原本还挂着 subtle-sidebar-glitch 动画，
       而那个动画写的是 transform。CSS 动画的优先级高于普通声明，
       会把抽屉的 translateX 整段盖掉，导致抽屉永远关不上。
       只保留动 box-shadow 的 sidebar-glow-breath，同时省掉满屏面板上
       的发光动画开销。 */
    animation: sidebar-glow-breath 10s infinite ease-in-out;
  }

  .sidebar.left {
    left: 0;
    transform: translateX(-100%);
  }

  .sidebar.right {
    right: 0;
    transform: translateX(100%);
  }

  .sidebar.is-open {
    transform: translateX(0);
    visibility: visible;
  }

  .drawer-toggle {
    display: flex;
  }
```

**`order: 2` / `order: 3` 与横向导航那几条必须删掉**：抽屉里导航要恢复竖排，而 `.left .nav-link` 的 `flex: 1 1 96px` 会把竖排的导航项拉成等宽横条。

- [ ] **Step 3: 确认没有残留的 order 规则**

Run: `grep -n "order:" src/components/layout/DefaultLayout.css`
Expected: 无输出（该文件里 `.vf-main` 的 `order: 1` 已随上一段一并删除）

- [ ] **Step 4: 手工验收**

Run: `pnpm dev`，然后打开 `http://localhost:3001`

在浏览器 DevTools 里切到设备模拟，逐条确认：

| 宽度 | 预期 |
| --- | --- |
| 1400px（桌面） | 左上与右上**没有** `☰` / `♪` 按钮；三栏 grid 布局与此前完全一致 |
| 1024px | 同上，三栏收窄版本 |
| 390px（手机横竖） | 出现 `☰` 与 `♪`；页面一进来就能看到它们，不需要滚动 |
| 390px 点 `☰` | 导航从左侧滑入，含 About；背景变暗；再点 `☰` 或点暗处收起 |
| 390px 点 `♪` | 播放器从右侧滑入；`☰` 抽屉自动收起 |
| 390px 开抽屉后按 Esc | 收起 |
| 390px 开抽屉后把宽度拖到 1200px | 抽屉自动收起、遮罩消失、body 恢复滚动 |
| 390px 开抽屉后在抽屉里点 About | 跳到 About 页且抽屉已收起 |

**特别确认**：在 390px 下点 `♪` 打开播放器，点 ▶ 能播放、◀◀ / ▶▶ 能切歌。这是验证「抽屉没有把播放器元素移出 DOM」的直接证据。

- [ ] **Step 5: 提交**

**需用户明确同意后执行**：

```bash
git add src/components/layout/DefaultLayout.css
git commit -m "style: 两个侧栏在窄屏改为侧拉抽屉"
```

---

### Task 5: 验证守卫用例真的会失败

**Files:**
- Modify: `src/components/layout/DefaultLayout.vue`（临时改动，验证后必须还原）

**Interfaces:**
- Consumes: Task 3 写进 `DefaultLayout.test.js` 的守卫用例
- Produces: 无代码变更 —— 本任务只做验伪，不产生提交

一条从不失败的守卫测试等于没有守卫。这个任务单独列出来，是因为它要临时改生产代码再还原，需要一个独立的复核点：**确认没还原干净，比没写这条测试更危险**。

- [ ] **Step 1: 临时给右侧栏加上 v-if**

临时把 `src/components/layout/DefaultLayout.vue` 里右侧栏那一行改成：

```html
      <aside v-if="openDrawer === 'player'" class="sidebar right" :class="{ 'is-open': openDrawer === 'player' }">
```

Run: `pnpm test src/components/layout/DefaultLayout.test.js`
Expected: FAIL —— 找不到 `.music-player audio`

**改完立刻还原**成 Task 3 的版本：

```html
      <aside class="sidebar right" :class="{ 'is-open': openDrawer === 'player' }">
```

再跑一次确认恢复绿色：`pnpm test src/components/layout/DefaultLayout.test.js` → PASS

- [ ] **Step 2: 确认工作区已还原**

Run: `git diff --stat src/components/layout/DefaultLayout.vue`
Expected: 无输出（`v-if` 已撤掉）

Run: `pnpm test src/components/layout/DefaultLayout.test.js`
Expected: PASS，10 个用例全绿

本任务不产生提交 —— 验伪结束后，脚本应当完全回到 Task 3 结束时的状态。

---

### Task 6: 修正 `AGENTS.md` 里两条过时的陷阱

**Files:**
- Modify: `AGENTS.md`（「已知陷阱」一节，`122-126` 行）

**Interfaces:**
- Consumes: 无
- Produces: 无

两条都已失效，依据如下：

1. 移动端拦截已于 2026-09-13（commit `504ad91`）全部移除。后端 `MobileBlockFilter`、`config/WebConfig.java`、`vvv/index.html` 的内联脚本三者都不在了；后端 `AGENTS.md:189` 已记录，前端这份没有同步。
2. `home/index.vue` 是**全项目唯一**使用 `@mouseenter` / `@mouseleave` 的文件，且它已经处理了触屏：`supportsHover()` 为假时 `infoVisible` 常显（`index.vue:150`）、拼图区块改为点击切换（`index.vue:160-162`）。原文说的「内容永远看不到」不成立。

- [ ] **Step 1: 替换「已知陷阱」里的前两条**

把 `AGENTS.md` 中：

```markdown
- **移动端拦截有两处**：后端 `MobileBlockFilter`，以及 `index.html` 里的**内联脚本**
  （用 `ontouchstart` + `innerWidth` 判定，命中会删掉 `#app` 再 `throw`）。
  只处理一处，手机端照样白屏。
- `home/index.vue` 等页面的悬浮信息栏用 `@mouseenter` / `@mouseleave` 控制，
  **移动端没有 hover，内容永远看不到**。
```

替换为：

```markdown
- **移动端拦截已在 2026-09-13 移除**（commit `504ad91`）：后端 `MobileBlockFilter`、
  `config/WebConfig.java` 与 `index.html` 里的内联脚本都不在了，手机端可正常访问。
  残留的化石不影响功能：`SecurityConfig` 白名单里还留着 `"/mobile-blocked.html"`，
  `ApiErrorController` 的 javadoc 还提到 `MobileBlockFilter`。
- `home/index.vue` 的悬浮信息栏是全项目唯一用 `@mouseenter` / `@mouseleave` 的地方，
  且已处理触屏：`supportsHover()` 为假时信息栏常显
  （`infoVisible` 的 `|| !hoverCapable`），拼图区块改走点击切换。
```

- [ ] **Step 2: 确认第三条仍然准确**

Run: `grep -n "latestBlogs\|pinnedBlog" src/modules/home/composables/useHomeContent.js src/views/home/index.vue`
Expected: 两处仍在读 `latestBlogs` / `pinnedBlog`，因此原文关于「后端从未返回、永远空值」的说法**保留不动**。

若发现该说法已不成立（例如后端补上了这两个字段），一并修正；否则本步骤不改任何内容，只确认。

- [ ] **Step 3: 提交**

**需用户明确同意后执行**：

```bash
git add AGENTS.md
git commit -m "docs: 修正已知陷阱里两条已过时的条目"
```

---

## 完成标准

全部任务完成后，以下命令必须全绿：

```bash
pnpm test
pnpm build
```

并满足：

- 桌面端（>768px）布局与改动前**逐像素一致**，没有出现 `☰` / `♪` 按钮
- 手机端（≤768px）不滚动就能看到并操作导航与音乐播放器
- 抽屉收起时，两个 `.sidebar`、播放器元素仍留在 DOM 中
- `grep -rn "v-if" src/components/layout/DefaultLayout.vue` 的结果里**没有**作用在 `.sidebar` 或 `.music-player` 上的分支

## 发布

纯前端展示改动，按 `CICD规范.md`「仅前端展示改动可只发布前端」，不需要动 `vvv_backend`，因此**不涉及联动分支**。

```bash
./scripts/deploy-current.sh --watch
```

发布后验证 `https://v1rtual.top/` 在手机宽度下导航与播放器可用。

## 明确不做

- 不改 `src/modules/player/composables/useAudioPlayer.js`
- 不改任何 Gallery / Profile 弹窗的 z-index
- 不删 `DefaultLayout.css` 里重复定义的 `@keyframes sidebar-glow-breath` / `subtle-sidebar-glitch`
- 不删 `src/utils/responsive.js` 的 `isNarrowViewport()`（本计划让它获得了调用方）
- 不处理 `/default-avatar.gif` 缺失导致的裂图问题，另行安排
